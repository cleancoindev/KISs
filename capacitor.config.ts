import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.crewsader.kiss',
  appName: 'KISs',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
